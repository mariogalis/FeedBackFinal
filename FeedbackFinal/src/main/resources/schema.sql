CREATE TABLE orders (
                        id BIGINT PRIMARY KEY AUTO_INCREMENT,
                        descripcion VARCHAR(255),
                        total DOUBLE,
                        fecha TIMESTAMP
);
